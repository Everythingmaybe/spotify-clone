{
  "version": 2,
  "builds": [{
    "src": "apps/server/open/src/main.ts",
    "use": "@vercel/node"
  }],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "apps/server/open/src/main.ts"
    }
  ]
}
