{
  "version": 2,
  "outputDirectory": ".",
  "builds": [{
    "src": "api/index.js",
    "use": "@vercel/node"
  }
  ],
  "routes": [{ "src": "api/(.*)", "dest": "api" }, { "src": "(.*)", "dest": "/" }]
}
